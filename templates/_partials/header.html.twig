<header>
    {% include '_partials/_usps.html.twig' %}
    <div class="container mx-auto mt-10 pb-5">
        <div class="flex gap-10">
            <a href="{{ path('app_home') }}">
                <img class="logo_full" width="200" src="{{ asset('build/assets/images/logo.png'|cdn('resize', 200, -1)) }}" alt="logo">
            </a>
            <div class="grow items-end flex">
                <input type="search" placeholder="{{ 'Where are you looking for?'|trans }}" class="input input-bordered input-accent grow"/>
            </div>
            <div class="flex gap-2 items-end">
                <a href="{{ path('app_customer') }}">
                    <i class="la la-user la-2-5x"></i>
                </a>
                <a href="#" class="indicator">
                    <span class="indicator-item badge badge-accent text-white p-2 aspect-square mt-2 mr-4 text-xs">9+</span>
                    <i class="la la-heart la-2-5x"></i>
                </a>
                <a href="{{ path('app_checkout_cart') }}" class="indicator">
                    <small class="indicator-item badge badge-accent text-white p-2 aspect-square mt-2 mr-4 text-xs">9</small>
                    <i class="la la-shopping-basket la-2-5x"></i>
                </a>
            </div>
        </div>
    </div>
    {% include '_partials/_navigation.html.twig' %}
</header>